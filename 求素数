//求素数// 
/*打印指定区间的全部素数 
taocyber      2021.4.1*/

#include<stdio.h>
int main()
{
    int i,j,n=0;// 定义两个整型变量 n的值为0 
    for(i=100;i<200;i++)//for循环  （变量i初次赋值；变量i的条件表达式；i的循环） 
    {
        j=2;//循环体内循环 j赋值为2 
        while(i%j!=0)//while 循环  表达式：i值除以j值的余数不等于0 
            j++;//j值加1，然后重复上述循环 
        if(i==j)//当上述循环结束后，判定i值是否等于j 值 
        {
            printf("%4d",i);//如果i=j，则打印i值，每个i值占 4个空格 
            n++;//n的值加1 
            if(n%8==0)//判定n除以8的余数是否等于0 
                printf("\n");//如果余数等于0，则打印空格并换行 
        }
    }
    printf("\n");//for循环内部循环结束时打印空格 并返回for循环开始的下一轮循环，直到i值等于200停止循环 
    return 0;
}
